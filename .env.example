# Application / Alkalmazás
PORT=3030
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
APP_VERSION=6.5.0
GIT_COMMIT_SHA=

# Persistent storage / Tartós tárhely
DB_PATH=./server/db/klavierhaus_v6.sqlite
BACKUP_DIR=./server/backups
UPLOAD_DIR=./server/uploads

# System Activation & Integrations encrypted secret storage.
# REQUIRED in production. Independent random 32+ character value; NEVER reuse JWT_SECRET.
# Production környezetben KÖTELEZŐ. Különálló 32+ karakteres véletlen érték; JWT_SECRET nem használható.
SYSTEM_INTEGRATION_ENCRYPTION_KEY=

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@klavierhaus.com
CRON_SECRET=replace-with-a-random-cron-secret

# Google Calendar operational direction remains Google -> ERP.
# The System Activation test requests event-write scope only to create and immediately delete its temporary test event.
APP_BASE_URL=https://YOUR-RENDER-DOMAIN
WEBSITE_BASE_URL=https://klavierhaus-home.onrender.com
WEBSITE_AUTO_INSTALL_SAMPLES=true
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_TOKEN_ENCRYPTION_KEY=
GOOGLE_CALENDAR_CENTRAL_EMAIL=klavierhauswork@gmail.com
GOOGLE_CALENDAR_ID=klavierhauswork@gmail.com
GOOGLE_CALENDAR_POLL_INTERVAL_MS=120000
GOOGLE_CALENDAR_INITIAL_LOOKBACK_DAYS=30
# GOOGLE_REDIRECT_URI=https://YOUR-RENDER-DOMAIN/api/google-calendar/oauth/callback
# GOOGLE_CALENDAR_WEBHOOK_URL=https://YOUR-RENDER-DOMAIN/api/google-calendar/webhook

# Resend environment values remain supported fallbacks. Encrypted DB key takes precedence at process startup when configured.
RESEND_API_KEY=
EMAIL_FROM=Klavierhaus Accounts <accounts@klavierhaus.com>
EVENT_EMAIL_FROM=Klavierhaus Events <events@klavierhaus.com>
EMAIL_REPLY_TO=
RESEND_WEBHOOK_SECRET=

WEBSITE_DEVICE_SECRET=
# Legacy marketing-token encryption key. New unified integration secrets use SYSTEM_INTEGRATION_ENCRYPTION_KEY.
MARKETING_TOKEN_ENCRYPTION_KEY=

# Stripe Sandbox only. Encrypted DB key takes precedence at process startup when configured.
STRIPE_SECRET_KEY=sk_test_replace_with_your_stripe_sandbox_secret
STRIPE_WEBHOOK_SECRET=whsec_replace_with_your_stripe_webhook_secret
